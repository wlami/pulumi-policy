# Pulumi Policy SDK (Java)

Author Pulumi policy packs in Java. See `../../docs/` in the umbrella `pulumi-policy-java` repo for the design.

## Build

    mvn -DskipTests=false package

## Test

    mvn test
